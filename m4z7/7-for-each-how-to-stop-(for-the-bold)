// for each how to stop (for the bold)

const arr = [5, 7, 4, 3, 2, 1, 2, 3, 4, 2, 2, 4, 5, 3]

try {
  arr.forEach(el => {
    if (el < 3) {
      throw new Error("element less than 3")
    }
    console.log(el)
  })
} catch (e) {
  console.log(`Loop has stopped because ${e.toString()}`)
}